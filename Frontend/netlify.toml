[build]
  command = "npm install && npm run build"
  publish = "dist"
  # Set VITE_BACKEND_URL in Netlify env vars: https://YOUR-RENDER-APP.onrender.com/api

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
